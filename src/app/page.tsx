export default function HomePage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <p className="text-clay-300 text-sm uppercase tracking-[0.2em]">APRIM</p>
      <h1 className="font-display mt-4 text-5xl text-teal-700">
        Backend infrastructure ready.
      </h1>
      <p className="mt-6 text-base text-teal-700/80">
        The APRIM site scaffolding is in place. Drop the chosen design direction
        into the page components — the data layer, form endpoints, email
        notifications, and content schema are wired up and waiting.
      </p>
    </main>
  );
}
